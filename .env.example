# Copy to .env.local for local development. NEVER commit real values.
# NOTE: VITE_ prefixed variables are embedded in the public build (see CONTRIBUTING.md).

# Google API key used for Google Drive backup. Must have HTTP referrer restrictions
# configured in Google Cloud Console (see README > Security).
VITE_GOOGLE_API_KEY=
