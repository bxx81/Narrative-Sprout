# Normalize line endings to LF in the repository (CI and Pages run on Linux).
* text=auto eol=lf

# Binary assets (never touch these)
*.webp binary
*.png binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
*.otf binary
