# Dependencies
node_modules/

# Build output — never commit (§7: Pages builds from source)
dist/
dist-ssr/

# Secrets — never commit (§3.1)
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
*.local

# Logs
*.log

# Playwright E2E output — never commit
playwright-report/
test-results/

# Tauri (src-tauri/ lives in main; see REDESIGN §3.4)
src-tauri/target/
src-tauri/gen/
